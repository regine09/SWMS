import 'package:flutter/material.dart';
import 'package:smws/utilities/pick_up.dart';

class PickupScreens extends StatelessWidget {
  const PickupScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: (
        Text('heloo')
      ),
    );
  }
}